import router from '@/router'
import * as types from '../types'

const state = {

};

const getters = {

};

const actions = {
  //登录
  login (store,sendData) {
  },

};

const mutations = {

};

export default {
  state,
  getters,
  actions,
  mutations
}
